"""Vendored modules — copies synchronisees de contrats partages.

Ces fichiers sont vendorises (copies) plutot que packages parce que les
projets sources ne sont pas encore en release pip stable. Synchroniser
manuellement quand la source evolue (voir VENDORED_FROM.md).
"""
